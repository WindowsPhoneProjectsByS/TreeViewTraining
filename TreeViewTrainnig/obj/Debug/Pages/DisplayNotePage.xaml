<Page
    x:Class="TreeViewTrainnig.Pages.DisplayNotePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TreeViewTrainnig.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>

            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="Notatki" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Text="Wyświetl" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="LocalizationInfo" Grid.Row="0" FontSize="25"
                       Text="Lokalizacja: " Margin="0,0,0,10"/>
            <TextBlock x:Name="NameInfo" Grid.Row="1" FontSize="25" 
                       Text="Nazwa: " Margin="0,0,0,10"/>
            <TextBox x:Name="ContentDisplay" TextWrapping="Wrap" VerticalAlignment="Stretch"
                       Grid.Row="2" Text="Zawrtość" FontSize="20" Padding="20,10,20,10"
                     />
            <Button x:ConnectionId='1' x:Name="ModficationButton" Grid.Row="3" Content="Modyfikuj" 
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"                                />
            <Button x:ConnectionId='2' x:Name="DeleteButton" Grid.Row="4" Content="Usuń" 
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"                           />
        </Grid>
    </Grid>
</Page>

